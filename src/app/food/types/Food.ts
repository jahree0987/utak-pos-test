export type Food = {
    name: string; 
    description?: string; 
    price: string; 
    category?: string;
    cost?: string; 
    stock: string; 
    image: string;
    sizes?: any;
    keys?: string;
    categoryKeys?: string;
}